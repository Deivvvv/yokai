using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LostDataManager : MonoBehaviour
{
    /*
     скрипт для храения не найденых данных и ссылок на то где требуется
    требуется заменить указатель или убрать условие
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
